import { expect } from 'chai';

describe("Math functions", () => {

      const a = 4
        const b = 5
        const c = 9
        const d = -1

    it("a+b=c", () => {
        expect(a+b).to.eq(c)
    })
    it("a-b=d", () => {
        expect(a-b).to.eq(d)
    })
})


